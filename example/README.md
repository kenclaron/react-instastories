# SPA with React Instastories library

> It is linked to the `react-instastories` package in the parent directory for development purposes.

## Development

```text
npm install
npm run start
npm run test
```
