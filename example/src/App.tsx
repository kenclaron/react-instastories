import React from "react";
import { ExampleComponent } from "react-instastories";
import "react-instastories/dist/index.css";

const App = () => {
  return (
    <ExampleComponent text="Hi, it is initial component for future React Instastories library. It is needed for checking tools for building this react-module." />
  );
};

export default App;
